# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Parse Foursquare Clone' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Parse Foursquare Clone
pod 'Parse'



  target 'Parse Foursquare CloneTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Parse Foursquare CloneUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
